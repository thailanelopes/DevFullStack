{
  "name": "gataria-frotend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "author": "Thailane Lopes Dutra",
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.5.0",
    "@testing-library/user-event": "^7.2.1",
    "react": "^16.3.1",
    "react-dom": "^16.3.1",
    "react-phone-input-2": "^2.15.1",
    "react-router-dom": "^5.3.4",
    "react-scripts": "^2.1.3"
  },
  "scripts": {
    "start": "NODE_OPTIONS=--openssl-legacy-provider react-scripts start",
    "start:dev": "NODE_OPTIONS=--openssl-legacy-provider REACT_APP_STAGE=dev react-scripts start",
    "start:prod": "NODE_OPTIONS=--openssl-legacy-provider REACT_APP_STAGE=prod react-scripts start",
    "build": "NODE_OPTIONS=--openssl-legacy-provider react-scripts build",
    "build:dev": "NODE_OPTIONS=--openssl-legacy-provider REACT_APP_STAGE=dev react-scripts build",
    "build:prod": "NODE_OPTIONS=--openssl-legacy-provider REACT_APP_STAGE=prod react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
